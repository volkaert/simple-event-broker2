server:
  port : 8081
spring:
  application:
    name: simple-event-broker2-publication-gateway
  cloud:
    gateway:
      routes:
        - id: standard-publication-adapter
          uri: http://localhost:8082
          predicates:
          - Path=/events
management:
  endpoints:
    web:
      exposure:
        include: "*"
